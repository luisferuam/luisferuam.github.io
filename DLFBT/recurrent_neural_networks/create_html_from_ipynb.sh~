# Pasos para crear el html

# 1. Convertir a md, las imágenes se guardan en la carpeta myMediaFolder
pandoc --extract-media ./myMediaFolder recurrent_neural_networks.ipynb -o recurrent_neural_networks.md

# 2. Eliminar las líneas que empiezan por ::: usando sed. En este paso se pueden hacer otras transformaciones
sed -i '/:::/d' recurrent_neural_networks.md

# 3. Añadir los metadatos, como el título, el autor o la fecha
cat recurrent_neural_networks_metadata recurrent_neural_networks.md > recurrent_neural_networks_with_title.md

# 4. Convertir el md en html, uso la plantilla myTemplate.html5
pandoc --data-dir=. --template=../templates/myTemplate.html5 --toc --mathjax --highlight-style tango recurrent_neural_networks_with_title.md -o recurrent_neural_networks.html

# 5. Algunas transformaciones adicionales
sed -i 's/<h1 class="toc-title">Contents<\/h1>/<h2 class="toc-title"><font color="#CA3532">Contents<\/font><\/h2>/g' recurrent_neural_networks.html
